---
title: "TypeScript narrowing patterns I actually use"
titleTh: "แพตเทิร์นการ narrow ใน TypeScript ที่ผมใช้จริง"
date: "2026-02-28"
excerpt: "Skip the encyclopedia. These five hold up in real codebases."
excerptTh: "ไม่ต้องท่องสารานุกรม ห้าแพตเทิร์นนี้ใช้ได้จริงในโค้ดจริง"
tags: ["typescript", "programming"]
---

There are dozens of TypeScript narrowing tricks. I use five of them every
day. The rest live in blog posts I bookmarked once and never opened again.

มีเทคนิคการ narrow ใน TypeScript เป็นสิบ ๆ แบบ ผมใช้จริงแค่ 5 อย่าง
ที่เหลือไปอยู่ในบล็อกที่ผม bookmark ไว้ครั้งเดียวแล้วไม่เปิดอีก

## 1. Discriminated unions

```ts
type Result<T> =
  | { ok: true; value: T }
  | { ok: false; error: Error };

function unwrap<T>(r: Result<T>): T {
  if (r.ok) return r.value;   // narrowed
  throw r.error;              // narrowed the other way
}
```

## 2. `in` for shape checks

```ts
function area(s: { width: number; height: number } | { radius: number }) {
  if ("radius" in s) return Math.PI * s.radius ** 2;
  return s.width * s.height;
}
```

The other three (assertion functions, type predicates, `satisfies`) are also
worth knowing — but if you only learn the first two, you'll get 80% of the
mileage.
